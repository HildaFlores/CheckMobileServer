# The database user to be used to connect to the database.
database_user=transaccional

# The password of the database user to be used to connect to the database.
database_password=oracle

# The server ip where the database is running.
server_ip=127.0.0.1

# The tcp port where the database is rinning.
database_port=1521

# The service to access the database.
database_service=xe